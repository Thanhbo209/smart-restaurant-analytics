spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:5432/${POSTGRES_DB:restaurant_analytics_test}
    username: ${POSTGRES_USER:test_user}
    password: ${POSTGRES_PASSWORD:test_password}
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
